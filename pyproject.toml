[project]
name = "firstproj"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Ksukskss", email = "kseniapogolelova@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
firstproj = "firstproj:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
